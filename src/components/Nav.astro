---
import Icon from './Icon.astro'

const { pathname } = Astro.url
---

<div>
  <nav>
    <ul>
      <li class:list={[{ active: pathname === '/' }]}>
        <a href="/"><Icon name="home" label="Home" /></a>
      </li>
      <li class:list={[{ active: pathname.includes('about') }]}>
        <a href="/about"><Icon name="clipboard" label="About" /></a>
      </li>
      <!-- <li class:list={[{ active: pathname === '/uses' }]}>
        <a href="/uses"><Icon name="settings" label="Uses" /></a>
      </li> -->
    </ul>
  </nav>
</div>

<style>
  div {
    display: grid;
    align-items: center;
  }

  nav {
    display: inline-block;
    background-color: var(--color-accent-surface-3);
    /* border: 1px solid var(--color-accent-surface-2); */
    box-shadow: inset 0 2px 0 rgb(var(--dark-raw) / 0.2);
    margin-inline: auto;
    border-radius: 9999px;
  }

  nav > ul {
    margin: 0;
    padding: 2px;
    display: flex;
    align-items: center;
    justify-content: center;
    list-style-type: none;
    gap: 0;
    flex-wrap: wrap;
  }

  li {
    position: relative;
    display: grid;
    padding-block: var(--space-2xs);
    padding-inline: var(--space-m);
    border-radius: 9999px;
  }

  li.active::before {
    content: '';
    position: absolute;
    display: block;
    place-self: center;
    background-color: rgb(var(--light-raw) / 0.4);
    mix-blend-mode: multiply;
    /* opacity: 0.3; */
    height: 4px;
    width: 80%;
    bottom: 1px;
    border-top-left-radius: 9999px;
    border-top-right-radius: 9999px;
  }

  ul > li > a[href] {
    display: inline-flex;
    gap: var(--space-2xs);
    color: var(--text-2);
    text-decoration: none;
    font-size: var(--size-step-0);
    transition: all 0.2s ease-in-out;
    align-items: center;
  }

  ul > li > a[href]:hover {
    color: var(--text-1);
  }

  ul > li.active {
    background-color: rgb(var(--fg-raw) / 0.1);
    box-shadow: inset 0 0 2px rgb(var(--dark-raw) / 0.2);
  }

  ul > li.active > a[href] {
    color: var(--text-1);
  }

  .spacer {
    flex-grow: 1;
  }
</style>
