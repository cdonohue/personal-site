---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<nav class={`nav ${className}`}>
  <slot />
</nav>

<style>
  .nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-standard);
  }
</style>
